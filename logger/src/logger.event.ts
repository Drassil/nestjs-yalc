export enum LoggerEvent {
  QUERY_ERROR = 'logger.query_error',
  QUERY_LOG = 'logger.query_log',
  QUERY_SLOW = 'logger.query_slow',
  ERROR = 'logger.error',
  SCHEMA_BUILD = 'logger.schema_build',
  WARN = 'logger.warn',
  INFO = 'logger.info',
  DEBUG = 'logger.debug',
  LOG = 'logger.log',
}
